# Pre-commit hooks for this module. Install once:
#   brew install pre-commit terraform-docs trivy checkov
#   pre-commit install
# Run on demand against everything:
#   pre-commit run -a
repos:
  - repo: https://github.com/antonbabenko/pre-commit-terraform
    rev: v1.96.1
    hooks:
      - id: terraform_fmt
      - id: terraform_validate
        args:
          - --tf-init-args=-backend=false
      - id: terraform_docs
        args:
          - --args=--config=.terraform-docs.yml
      # Trivy replaces tfsec, which Aqua Security has retired ("attention will be
      # directed at Trivy going forward"). tfsec's HCL parser also predates
      # Terraform 1.5 `check` blocks and fails outright on the name_lengths check.
      - id: terraform_trivy
      - id: terraform_checkov
        args:
          - --args=--framework terraform
          - --args=--compact
          - --args=--quiet

  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-merge-conflict
      - id: check-yaml
      - id: detect-private-key
