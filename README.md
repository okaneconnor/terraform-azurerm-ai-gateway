# terraform-azurerm-ai-gateway

Reusable Terraform module for a private, keyless, multi-service Azure AI gateway on Azure API Management.

The module is added via the open pull request from `feat/terraform-module`.
